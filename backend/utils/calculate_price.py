async def calculate_price(engine_id, salone_member_id, salone_option_ids, service_id, shassi_ids, zip_ids):
    # base_price = get_base_price(model_id)
    # color_price = get_color_price(color_id)
    # trim_price = get_trim_price(trim_id)
    # options_price = sum(get_option_price(option_id) for option_id in option_ids)
    # total_price = base_price + color_price + trim_price + options_price
    # return total_price
    pass